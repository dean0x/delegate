# Compact Status - 2025-11-21

**Focus**: Incremental Graph Updates + Test Infrastructure Safety

**Key Accomplishments**:
1. Implemented incremental graph updates (70-80% latency reduction)
2. Fixed memory leak in `removeEdge()` (phantom empty Set cleanup)
3. Technical safeguard: `npm test` now blocked to prevent Claude Code crashes

**Critical Decisions**:
- Block `npm test` with warning message (LLMs are non-deterministic, can't rely on documentation)
- Move graph ownership from repository to handler (event-driven architecture)
- Reduce all test memory limits to 2GB

**Next Priority Actions**:
1. Merge PR #27 after CI passes
2. Consider v0.3.2 release
3. Monitor test safeguard effectiveness

**Critical Issues**:
- Full test suite still crashes Claude Code (use grouped commands only)
- Issue #28 open for shallow copy bug (locked, will address later)

**Key Files Modified**:
- `src/core/dependency-graph.ts` - Incremental updates + memory leak fix
- `src/services/handlers/dependency-handler.ts` - Now owns graph
- `package.json` - Test safeguard, 2GB memory limits
- `vitest.config.ts` - Worker memory recycling at 1GB
- `CLAUDE.md` - Updated testing documentation

**Context Notes**:
Never run `npm test` from Claude Code - it crashes the instance. Use individual groups like `npm run test:core`. All tests use mocks; no real Claude processes are spawned. The memory leak was subtle: `addEdgeInternal()` creates phantom empty Sets that weren't cleaned up.

---
*Quick summary of [full status](./../2025-11-21_0601.md)*
